## Packages
framer-motion | Page transitions and layout animations
recharts | Data visualization for whip counts and swing analysis
clsx | Conditional class names utility
tailwind-merge | Utility for merging tailwind classes

## Notes
- Using ProPublica API data structure
- API endpoints are defined in shared/routes
- Authentication is currently open (no login required for MVP)
- Tailwind config needs to support 'font-serif' for Playfair Display and 'font-sans' for Inter
